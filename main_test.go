package main

import "testing"

func TestRootPackage_Builds(t *testing.T) {
	// Verifies the root ecdisk package compiles successfully.
	// The actual main() function delegates to app.Main(),
	// so we only verify the package builds rather than invoking main().
}
