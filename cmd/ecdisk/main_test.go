package main

import "testing"

func TestMainPackage_Builds(t *testing.T) {
	// Verifies the cmd/ecdisk package compiles successfully.
	// The actual main() function calls app.Main() which reads os.Args,
	// so we only verify the package builds rather than invoking main().
}
